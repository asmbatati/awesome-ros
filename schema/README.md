# Schema Definitions

This directory contains JSON Schema files that define the structure and validation rules for the CSV datasets.

- **`papers.schema.json`** — Schema for `data/papers.csv`
- **`frameworks.schema.json`** — Schema for `data/frameworks.csv`

These schemas are used by the CI pipeline (`.github/workflows/validate.yml`) to automatically validate contributions.
